{# src/Blogger/SecurityBundle/Resources/views/User/edit.html.twig #}
{% extends 'BloggerSecurityBundle::layout.html.twig' %}

{% block body %}
    <header>
        <h1>Edit your user information</h1>
    </header>

    {% if app.session.hasFlash('blogger-notice') %}
        <div class="blogger-notice">
            {{ app.session.flash('blogger-notice') }}
        </div>
    {% endif %}
    
    {% include 'BloggerSecurityBundle:User:form.html.twig' with { 'form': form } %}
{% endblock %}